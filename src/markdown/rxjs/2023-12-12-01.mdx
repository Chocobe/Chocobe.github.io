---
id: Rxjs 식별자 1
title: Rxjs 제목 1
description: 'Rxjs 테스트 문서<br />Hello World<br />안녕하센요'
thumbnail: https://image.dongascience.com/Photo/2020/03/5bddba7b6574b95d37b6079c199d7101.jpg

featured: true
tags: []

createdAt: '2023-11-18 20:12:33'
modifiedHistories: []
---

# Rxjs 테스트 문서

```typescript showLineNumbers
function sum(lhs: number, rhs: number) {
    return lhs + rhs;
}

const sub = (lhs: number, rhs: number) => {
    return lhs - rhs;
}

const myCalculator = (a: number, b: number, c: number, d: number, e: number, f: number, g: number): number => {
    return a + b + c + d + e + f+ g;
}

const a = sum(1, 2);
const b = sub(4, 3);

console.group('Rxjs 테스트 문서');
console.log('a: ', a);
console.log('b: ', b);
console.groupEnd();
```

안녕하세요.
